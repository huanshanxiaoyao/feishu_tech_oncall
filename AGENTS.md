# tech_oncall 环境与设计约定

这份文档记录排查 Agent（第二步）依赖的环境事实和产品设计约定，跟代码本身分开维护，
方便随时对照。技术实现细节仍以 `README.md` / 代码 / 计划文档为准，这里只记外部事实。

## 目录约定

- 本项目工作目录：`/opt/workspace/tech_oncall`
- 默认排查的业务：**plum**
  - 后端：`/opt/workspace/ai4all_bridge`
  - 前端：`/opt/workspace/plum_chat`
  - 管理后台：`/opt/workspace/plum_admin`

## 权限模型

- 以上三个目录、其数据库、日志、服务运行情况、系统/网络状态，默认**全部只读**——
  代码、配置、数据库数据、服务运行状态都要能读，但不能写、不能改、不能删。
- 唯一允许读写的地方是临时目录（例如 `/tmp/`），供 Agent 记笔记、暂存中间结果用。
- 这个只读边界必须在工具实现层强制（不依赖 prompt 约束），见代码里 `src/tools/`
  的设计说明。

## Git 状态

- `/opt/workspace/tech_oncall` 截至 2026-09-03 还没有初始化 git 仓库，稍后会加。
  在那之前不用假设有 `.gitignore`/远程仓库之类的东西。

## 产品目标

期望效果类似于：技术人员手动登录到这台线上机器，起一个 `claude`/`codex` 之类的
交互式 agent，自己去排查清楚一个线上问题的原因，然后把结论带回来——而不是被
限定在一个很窄的、预先枚举好的工具/场景列表里。所以 Agent 的调查范围要尽量宽
（代码、日志、数据库、服务状态、网络等都要能摸到），边界卡在"只读 vs 可写"，
不卡在"只能查哪几类问题"。

## 请求处理约定

- 每一次收到的排查请求都要在本地留痕（`user_name` 已经打通，见 `messages`/`cases` 表）。
- 收到排查指令后，先做一次"指令合理性判断"，再决定是否真的起 Agent 去排查：
  - **闲聊 / 不符合 tech_oncall 定位的请求**：不执行任何排查，直接回复——语气要
    像工程师之间聊天，带点幽默感，不要生硬拒绝。
  - **破坏性指令**（删文件、停线上服务等）：不执行，回复严肃警告；且对应用户
    （按 open_id）在 1 小时内不再受理新的排查请求。
  - 其余情况才进入正常排查流程。

（触发这份文档更新的原始约定见 2026-09-03 的对话记录；具体实现方案见对应的计划文档。）
